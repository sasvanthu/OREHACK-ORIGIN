// ActiveHackathons section — scroll animation removed, 3D canvas removed.
// Kept as a lightweight spacer/anchor for the #hackathons ID if needed.
export default function ActiveHackathons() {
  return <div id="hackathons" />;
}
